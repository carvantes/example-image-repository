/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/andy-base-path',
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'exiv2.org'],
  },
};

module.exports = nextConfig
